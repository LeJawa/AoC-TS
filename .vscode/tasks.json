{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "command": "npm run build",
            "problemMatcher": ["$tsc"],
            "group": "build",
            "label": "tsc: build",
            "options": {
                "shell": { "executable": "Powershell" }
            }
        }
    ]
}
